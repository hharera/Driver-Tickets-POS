apply from: "$rootDir/${CommonBuild.FeatureModule}"

dependencies {
    api(project(Features.login))
    api(project(Features.ticket))
    api(project(Features.dayReport))
    api(project(Features.endShift))
    api(project(Features.scanReservedTicket))


    api((Libs.navigationFragment))
    api((Libs.navigationUi))
}